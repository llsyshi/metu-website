# METU 官网项目启动说明

## 项目技术栈

- Vue 3
- Vite
- Tailwind CSS
- 纯前端静态网站，无后端

## 第一次启动

在项目根目录打开 PowerShell：

```powershell
cd E:\desktop\metu-website
npm install
npm run dev -- --port 5173
```

启动成功后，在浏览器打开：

```text
http://localhost:5173/
```

## 日常启动

如果已经安装过依赖，后续只需要运行：

```powershell
cd E:\desktop\metu-website
npm run dev -- --port 5173
```

也可以直接运行项目根目录下的脚本：

```powershell
.\start-dev.ps1
```

如果系统提示脚本执行策略限制，可以用下面的方式运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\start-dev.ps1
```

## 打包发布

如果需要生成可部署的静态文件：

```powershell
npm run build
```

打包结果会生成在：

```text
dist/
```

## 常见问题

### 1. 端口被占用

默认端口是 `5173`。如果端口被占用，可以换一个端口：

```powershell
npm run dev -- --port 5174
```

### 2. 页面打不开

先确认启动命令窗口没有关闭，并且终端里出现类似下面的信息：

```text
Local: http://localhost:5173/
```

然后再打开浏览器访问 `http://localhost:5173/`。

### 3. 修改图片和文案

主要页面代码在：

```text
src/App.vue
```

全局样式在：

```text
src/style.css
```

产品图片目前使用 `图片/` 文件夹里的占位照片，后续可以替换为真实产品图、工厂图和 Logo。
